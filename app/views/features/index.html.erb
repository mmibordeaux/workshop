<h1>Caractéristiques</h1>

<div class="row">
  <% @features.each do |feature| %>
    <div class="col-md-2">
      <%= link_to feature do %>
        <div class="feature-badge">
          <i class="glyphicon <%= feature.icon %>"></i>
          <label><%= feature %></label>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
