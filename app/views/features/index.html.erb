<% @fields.each do |field| %>
  <h2><%= field %></h2>
  <div class="row">
    <% field.features.each do |feature| %>
      <div class="col-md-2">
        <%= render partial: 'badge', locals: { feature: feature } %>
      </div>
    <% end %>
  </div>
<% end %>
