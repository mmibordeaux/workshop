<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>mmi workshop</title>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= csrf_meta_tags %>
</head>
<body>
    <nav class="navbar navbar-default navbar-static-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Masquer la navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="/">mmi workshop</a>
            </div>
            <div id="navbar" class="navbar-collapse collapse">
                <ul class="nav navbar-nav navbar-right">
                    <% if user_signed_in? %>
                    <li><%= link_to 'Projets', projects_path if can? :read, :projects %></li>
                    <li><%= link_to 'Utilisateurs', users_path if can? :read, :users %></li>
                    <li><%= link_to 'Caractéristiques', features_path if can? :read, :features %></li>
                    <% else %>
                    <li><%= link_to "Connexion", new_session_path(resource_name) %></li>
                    <li><%= link_to "Inscription", new_registration_path(resource_name) %></li>
                    <% end %>
                </ul>
            </div>
        </div>
    </nav>
    <div class="jumbotron">
        mmi workshop
    </div>
    <div class="container">
        <p id="notice"><%= notice %></p>
        <%= yield %>
    </div>
</body>
</html>
