<%= simple_form_for(@project) do |f| %>
  <%= f.error_notification %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :url %>
  <%= f.input :github %>
  <%= f.association :features, 
        as: :check_boxes, 
        label: 'Caractéristiques', 
        collection: Feature.includes(:field).reorder('fields.parent_id, features.field_id, level').all.map { |f| [f.name_with_hierarchy, f.id] } %>
  <%= f.button :submit, class: "btn btn-default" %>
<% end %>
