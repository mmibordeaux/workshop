<%= link_to 'New', new_project_path, class: 'btn btn-primary' if can? :create, Project %>
<div class="row">
  <% @projects.each do |project| %>
    <div class="col-md-3">
      <%= link_to project do %>
        <%= render 'project', project: project %>
      <% end %>
    </div>
  <% end %>
</div>
